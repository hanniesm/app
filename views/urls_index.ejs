<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">

<!--   Add EJS code here -->
<% include ./partials/_header %>
  <!-- This link isn't working obvs -->
  <% if (username) {%>
    <a href='/urls/new'>Create New Tiny URL</a>
  <h2>My URLs</h2>
  <ul>
    <% for (var url in urls) { %>
        <% const urlPath = "/urls/" + url%>
        <% const deletePath = "/urls/" + url + "/delete" %>
      <li><%= url %>: <%= urls[url].longURL %>
        <form class="form-inline" method="POST" action="<%=deletePath%>">
          <button type="submit" name="shortURL" value="<%=url%>">Delete
          </button>
        </form>
        <% if (username) {%>
          <a href=<%=urlPath%>>Edit</a>
        <% } %>
      </li>
    <% }; %>
  </ul>
<% } %>

</body>
</html>

  <!-- let templateVars = { urls: urlDatabase }; -->
